name: Continuous Integration

on:
  push:
    branches: [ "main", "v2", "develop" ]
  pull_request:
    branches: [ "main", "v2", "develop" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Run Tests
      run: echo "Running integration and code coverage tests..."
