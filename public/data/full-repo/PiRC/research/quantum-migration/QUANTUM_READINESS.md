# Quantum Migration Pipeline

## Trigger Conditions
We observe NIST PQ cryptography standards. Migration of funds and key rotation inside Soroban vaults (PiRC-303) will be initiated if Shor's algorithm feasibility drops below the 5-year projection horizon.

## Validation Suites
See `Contracts/legacy_core/src/crypto.rs` for the Falcon512 and Dilithium SDK hook structures currently being tested on Termux local nodes.
